# Casks in this tap are generated by GoReleaser, whose formatting trips two purely cosmetic
# Homebrew layout cops. Disable just those — `brew audit` (the meaningful check) still runs.
Layout/ArgumentAlignment:
  Enabled: false
Layout/EmptyLinesAroundBlockBody:
  Enabled: false